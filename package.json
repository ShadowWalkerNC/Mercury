{
  "name": "mercury",
  "version": "0.0.1",
  "private": true,
  "description": "Self-hosted communication platform — monorepo root",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prebuild": "npm run build --workspace=packages/shared",
    "build": "npm run build --workspace=packages/server && npm run build --workspace=packages/web",
    "dev": "npm run dev --workspace=packages/server & npm run dev --workspace=packages/web",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
